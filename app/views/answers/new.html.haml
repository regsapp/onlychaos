.page-header
  %h1
    - if @answer.test
      = "Question #{@answer.number} of #{@answer.test_questions_count}"
    - else
      New Answer

= render 'form'

= link_to answers_path, :class => 'normal button' do
  %span.glyphicon.glyphicon-list-alt
  Back
