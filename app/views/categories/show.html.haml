.page-header
  = link_to categories_path, :class => 'normal button' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_category_path(@category), :class => 'normal button' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show category

%dl.dl-horizontal
  %dt Name:
  %dd= @category.name
